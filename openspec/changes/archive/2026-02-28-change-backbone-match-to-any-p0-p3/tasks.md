## 1. Backbone Matching Rule

- [x] 1.1 将共享骨架比较逻辑从 `P0/P1/P2/P3` 四点全匹配改为任意一个同角色历史点位匹配即命中
- [x] 1.2 保持同一 `P4` bar 内成功下单后不再重复下单的现有规则不变
- [x] 1.3 保持同骨架在首次成功前可跨多个 `P4` bar 继续尝试、首次成功后后续 `P4` bar 不再下单的现有高层语义

## 2. Logging And Diagnostics

- [x] 2.1 更新共享骨架阻止日志，显示命中的同角色重叠历史点位（如 `P0`、`P1`、`P2` 或 `P3`）
- [x] 2.2 确认日志能够区分“同 bar 成功锁”和“共享骨架成功锁”两类阻止原因

## 3. Verification

- [x] 3.1 编译 EA，确认共享骨架比较规则放宽后没有引入构建错误
- [x] 3.2 在 Strategy Tester 中验证只共享单个同角色历史点位的两个骨架，在首次成功后会触发后续 `P4` bar 禁止下单
- [x] 3.3 在 Strategy Tester 中验证跨角色共享同一根 K 线柱不会触发共享骨架锁，且完全不共享 `P0/P1/P2/P3` 的骨架仍可彼此独立下单
