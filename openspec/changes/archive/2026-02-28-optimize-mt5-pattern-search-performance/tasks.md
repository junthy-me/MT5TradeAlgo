## 1. 缓存与状态结构

- [x] 1.1 为每个交易品种新增历史结构缓存，记录最近一次已收盘 bar 状态和可复用的历史候选结构
- [x] 1.2 在运行时流程中增加“是否有新收盘 bar”的检测逻辑，用于决定是否刷新历史缓存

## 2. 搜索路径重构

- [x] 2.1 将 P0-P3 历史结构搜索从实时 P4 触发评估中拆分出来
- [x] 2.2 实现候选极值点预提取或等效预筛选逻辑，减少大跨度参数下的组合搜索规模
- [x] 2.3 在候选组合阶段增加轻量剪枝，尽早排除明显无效的点位组合
- [x] 2.4 将高频 `OnTimer` 路径改为“缓存更新 + 实时 P4 评估 + 持仓管理”，避免每次全量历史重算

## 3. 结果一致性与性能验证

- [ ] 3.1 对比优化前后在相同输入下的模式匹配与交易决策结果，确认策略语义未发生意外变化
- [ ] 3.2 编译 EA，确认缓存与搜索重构没有引入构建错误
- [ ] 3.3 在 `InpAdjustPointMaxSpanKNumber = 5` 和 `100` 的场景下做回测耗时对比，确认大跨度参数的性能明显改善
