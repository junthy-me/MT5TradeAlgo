## 1. 点位取值重构

- [x] 1.1 重构 P0-P3 的点位取值逻辑，使 P0/P2 使用已收盘 K 线最低价，P1/P3 使用已收盘 K 线最高价
- [x] 1.2 重构 P4 的触发逻辑，使其使用当前实时价格参与模式计算，而不是等待当前 K 线收盘
- [x] 1.3 重构 P5/P6 的点位取值逻辑，使 P5 使用已收盘 K 线最低价，P6 使用已收盘 K 线最高价
- [x] 1.4 明确并实现该策略中角色化点位取值优先于 `PointValueTypeEnum` 的规则

## 2. 变量与条件修正

- [x] 2.1 基于新的点位来源重新计算 `a`、`b1`、`b2`、`c`、`d`、`e` 等空间变量
- [x] 2.2 调整 `CondE` 的 `tspanmin` 计算逻辑，使其仅基于 `min(t1, t2, t3)`，不包含 `t4`
- [x] 2.3 保持 `CondC` 原公式不变，并验证其与实时 P4 触发逻辑兼容

## 3. 日志与验证

- [x] 3.1 更新模式匹配和交易日志，明确各点位价格来源并区分 P4 的实时触发语义
- [ ] 3.2 编译 EA，确认角色化点位取值与新的时间变量定义没有引入构建错误
- [ ] 3.3 在 Strategy Tester 中验证历史结构识别、实时 P4 触发、`CondE` 排除 `t4`、以及 P5/P6 后续确认逻辑
