## 1. EA 骨架与配置

- [x] 1.1 创建 MT5 Expert Advisor 文件结构，并定义交易品种、周期、定时器间隔、魔术号、备注、手数、滑点、点位模式、比例阈值和持仓上限等输入参数
- [x] 1.2 实现运行时初始化与退出逻辑，包括品种列表解析和 `OnTimer` 定时器注册
- [x] 1.3 为模式快照、条件标记、交易价位和单品种持仓状态建立共享数据结构

## 2. 模式检测引擎

- [x] 2.1 实现市场数据辅助函数，用于读取已收盘 K 线，并提供最高价、最低价、均价和时间戳访问能力
- [x] 2.2 实现 P0-P6 候选点位提取逻辑，并通过 `AdjustPointMaxSpanKNumber` 强制限制相邻点跨度
- [x] 2.3 实现 `a`、`b1`、`b2`、`c`、`d`、`e`、`r1`、`r2`、`sspanmin`、`t1`-`t6`、`trigger_pattern_total_time_minute` 和 `tspanmin` 的空间与时间变量计算
- [x] 2.4 实现 CondA 到 CondF 校验逻辑，并仅在全部条件满足时输出可交易模式快照

## 3. 交易执行与风控

- [x] 3.1 实现买入开仓逻辑，根据已验证的模式快照推导 P4 参考入场价、强止损和止盈价位
- [x] 3.2 基于 magic number 和 comment 实现单品种 EA 持仓计数，并在超过 `InpMaxPositionsPerSymbol` 时阻止继续开仓
- [x] 3.3 实现开仓后对 P5 和 P6 的跟踪逻辑，包括 SetSoftLoss 条件判断和弱止损激活
- [x] 3.4 实现强止损、已激活弱止损和止盈触发后的平仓处理，按当前可执行市场价完成出场

## 4. 日志与验证

- [x] 4.1 实现结构化日志，记录匹配点位、空间变量、时间变量、衍生交易价位和订单提交细节
- [x] 4.2 实现平仓日志，为每次 EA 管理持仓的关闭记录出场原因和执行价格
- [ ] 4.3 在 MetaEditor 或等效工具链中编译 EA，并清除所有构建错误和警告
- [ ] 4.4 执行策略级验证场景，覆盖模式识别、多品种定时扫描、持仓上限、强止损、弱止损激活和止盈出场
