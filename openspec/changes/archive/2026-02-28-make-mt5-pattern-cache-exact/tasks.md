## 1. 完整缓存与结果语义

- [x] 1.1 去除历史候选固定上限与截断逻辑，改为完整候选缓存
- [x] 1.2 定义并实现与原始全量搜索版一致的最终候选选择优先级与 tie-break 规则
- [x] 1.3 让实时 `P4` 评估在完整历史候选集上完成后再选出最终候选，而不是第一个命中即返回

## 2. 严格等价优化

- [ ] 2.1 将历史缓存重构为索引骨架或等价完整结构，减少完整缓存的内存冗余
- [x] 2.2 保留新 bar 驱动的历史缓存刷新机制，确保无新收盘 bar 时不重复全量重建
- [x] 2.3 引入严格等价剪枝，至少覆盖跨度约束、基础角色关系与 CondE 历史时长约束
- [x] 2.4 基于 CondA 的精确可行区间实现更强的等价剪枝，减少无效 `P0` 组合计算

## 3. 一致性与性能验证

- [x] 3.1 构建原始全量搜索实现与优化实现的对照验证入口
- [ ] 3.2 在固定历史窗口与固定实时价格输入下，对比 `P0-P4`、空间变量、CondA-H 和交易结论完全一致
- [ ] 3.3 验证运行时不再出现历史候选截断现象
- [ ] 3.4 在保持结果完全一致的前提下，对比优化前后的回测耗时与资源占用
